export const checklist = [
  {
    section: "📁 Estructura del Proyecto",
    color: "#00D4FF",
    items: [
      "Arquitectura MVC en 3 capas: controllers / services / prisma (data access)",
      "Carpeta routes/ con archivos por recurso (usersRouter.js, postsRouter.js…)",
      "Carpeta middlewares/ para auth, validación y manejo de errores",
      "Carpeta config/ para Prisma, Passport y variables de entorno",
      "Carpeta tests/ con archivos *.test.js espejo del árbol de src/",
      ".gitignore correcto: node_modules, .env, generated/prisma",
    ],
  },
  {
    section: "🌍 Variables de Entorno",
    color: "#F97316",
    items: [
      "Archivo .env creado en la raíz del proyecto y añadido a .gitignore — nunca versionarlo",
      "Archivo .env.example commiteado con todas las keys documentadas pero sin valores reales",
      "Archivo .env.test separado exclusivamente para el entorno de pruebas (TEST_DATABASE_URL, etc.)",
      "Todas las variables siguen UPPER_SNAKE_CASE: DATABASE_URL, JWT_SECRET, NODE_ENV…",
      "NODE_ENV definido y usado para comportamiento diferenciado: 'development' / 'test' / 'production'",
      "Cero valores hardcodeados en el código — todo secreto o config vive en process.env",
      "Las variables de entorno son siempre strings: convertir con parseInt() o === 'true' cuando se necesite número o booleano",
      "Validación fail-fast al arrancar: si una variable requerida es undefined, la app lanza error antes de iniciar",
      "Variables de entorno documentadas en README.md: nombre, descripción, si es requerida u opcional y ejemplo de valor",
      "Verificar que .env no está siendo trackeado por Git: ejecutar git ls-files --error-unmatch .env debe fallar",
      "El servicio de deploy (Railway, Render, Heroku…) tiene configuradas las variables desde su dashboard, no desde archivos",
    ],
  },
  {
    section: "⚙️ Configuración del Servidor Express",
    color: "#38BDF8",
    items: [
      "app.js solo configura Express (middlewares, rutas, error handler) — sin app.listen()",
      "server.js (o index.js) es el único archivo que llama app.listen() — no se prueba con Jest",
      "Puerto leído desde variable de entorno con fallback: process.env.PORT || 3000",
      "app.listen() recibe callback con parámetro error: si existe, se lanza — nunca silenciar errores de arranque",
      "helmet() registrado como primer middleware antes que cualquier otro",
      "express.json() y express.urlencoded() registrados globalmente para parsear bodies",
      "Routers montados después de los middlewares globales y antes del error handler",
      "Middleware de error global (4 parámetros) registrado como último elemento del stack",
      "El orden de las rutas y routers importa: Express ejecuta la primera coincidencia con verbo + path",
      "Ruta 404 catch-all ({*splat}) definida después de todos los routers de recursos",
      "Cada ciclo request-response debe cerrarse explícitamente: res.send(), res.json(), res.redirect() — nunca dejar requests colgados",
      "app exportado como módulo desde app.js para poder importarlo en los tests con Supertest",
    ],
  },
  {
    section: "🔌 API REST & Versionado",
    color: "#A78BFA",
    items: [
      "Prefijo de versión en todas las rutas: /api/v1/…",
      "Recursos en plural y en sustantivos — nunca verbos en la URL (/posts no /getAllPosts, /users no /saveUserInDatabase)",
      "Dos URIs por recurso: colección (/posts) y elemento específico (/posts/:postId)",
      "Verbos HTTP correctos: GET=leer, POST=crear, PUT=actualizar completo, PATCH=parcial, DELETE=eliminar",
      "Cada verbo tiene su método Express propio: app.get(), app.post(), app.put(), app.patch(), app.delete()",
      "Anidamiento lógico de recursos (máx. 3 niveles): /posts/:postId/comments/:commentId",
      "Parámetros de ruta (req.params) para identificar recursos: /:userId, /:postId",
      "Parámetros de consulta (req.query) exclusivamente para filtros, orden y paginación: ?page=1&limit=20&sort=createdAt",
      "Nunca mezclar req.params con req.query: params identifican el recurso, query lo filtra u ordena",
      "Ruta catch-all al final del stack para 404 personalizado: app.get('/{*splat}', handler) — siempre la última ruta registrada",
      "Cada recurso tiene su propio Router en routes/: usersRouter.js, postsRouter.js…",
      "Los Routers se montan en app.js con app.use('/api/v1/users', usersRouter) — la ruta base NO se repite dentro del router",
      "El orden de montaje de routers importa: los más específicos antes que los genéricos",
      "Respuestas siempre en JSON con res.json() — nunca res.send() para APIs REST",
      "express.json() registrado como middleware global para parsear bodies JSON entrantes",
      "Estructura de respuesta consistente: { data, meta, error } — igual en todos los endpoints",
      "API diseñada para ser consumida por múltiples clientes: web, mobile, desktop — sin asumir el origen",
    ],
  },
  {
    section: "🔗 Middleware",
    color: "#FB7185",
    items: [
      "Todo middleware recibe (req, res, next) — si no llama a next() ni cierra el ciclo, el request queda colgado",
      "El middleware de error tiene exactamente 4 parámetros (err, req, res, next) — si falta uno, Express no lo reconoce como error handler",
      "Middleware global (app.use) se registra antes que los routers para que aplique a todas las rutas",
      "Middleware de router (router.use) solo aplica a las rutas de ese router — úsalo para agrupar lógica específica (auth de admin, etc.)",
      "El orden de registro determina el orden de ejecución — un middleware registrado después de una ruta no aplica a esa ruta",
      "next() sin argumentos → siguiente middleware; next(error) → salta directo al error handler; next('route') → siguiente handler de la misma ruta",
      "Los middlewares que modifican req (como passport, express.json) deben registrarse antes que cualquier ruta que los use",
      "res.locals se usa para pasar datos entre middlewares hacia las vistas — por ejemplo currentUser",
      "Los middlewares de terceros (helmet, cors, morgan) se registran en orden: seguridad primero, luego parsers, luego lógica de negocio",
      "Nunca llamar next() después de haber enviado una respuesta — causa el error 'Cannot set headers after they are sent'",
      "Extraer middlewares reutilizables (validate, authenticate, authorize) a src/middlewares/ — nunca definirlos inline en el router",
    ],
  },
  {
    section: "🎮 Controladores",
    color: "#C084FC",
    items: [
      "Los controladores son funciones con una única responsabilidad: manejar req y devolver res — sin lógica de negocio directa",
      "Controlador ≠ Middleware: los controladores son parte del patrón MVC, el middleware es una característica de Express",
      "Nomenclatura de controladores basada en verbo + recurso: getUsers, createPost, deleteComment, updateProfile",
      "Un controlador por acción: nunca un controlador que haga GET y POST a la vez",
      "Siempre usar res.json() en lugar de res.send() para respuestas JSON — es explícito y siempre serializa correctamente",
      "res.status() se encadena antes del método de respuesta: res.status(404).json({...}) — nunca al revés",
      "Cuando se detecta un caso de error (recurso no encontrado, etc.), hacer return después de enviar la respuesta para detener la ejecución",
      "En Express v5 los controladores async lanzan errores automáticamente a next(err) — no necesitan try/catch para eso, pero sí para control de flujo propio",
      "Los controladores no llaman a next() al final — lo hacen los middlewares intermedios. El controlador cierra el ciclo.",
      "req.params siempre devuelve strings: convertir a Number() o parseInt() antes de consultar la DB",
      "Los archivos de controladores se ubican en controllers/ y se importan en los routers — nunca definir lógica inline en el router",
    ],
  },
  {
    section: "🗄️ Base de Datos & PostgreSQL (pg)",
    color: "#34D399",
    items: [
      "PostgreSQL corriendo localmente y accesible — verificar con psql que el rol y su base de datos existen",
      "El rol de PostgreSQL tiene contraseña configurada con \\password <role> desde psql",
      "Las credenciales del rol guardadas en variables de entorno — nunca hardcodeadas en el código fuente ni en el historial de Git",
      "Pool de pg configurado en db/pool.js y exportado como singleton — nunca crear un Pool por request",
      "Usar Pool (no Client) para el servidor Express: gestiona múltiples conexiones concurrentes automáticamente",
      "Client usado solo en scripts puntuales (seed, migrate): conectar → ejecutar → cerrar explícitamente",
      "connectionString construida desde process.env.DATABASE_URL — soporta cambiar de entorno sin tocar código",
      "Todas las queries usan parametrización con $1, $2…: pool.query('SELECT * FROM t WHERE id = $1', [id]) — nunca concatenar inputs de usuario en SQL",
      "Funciones de acceso a datos en db/queries.js separadas de los controladores — los controladores solo llaman a estas funciones",
      "Script de seed en db/populatedb.js que acepta la DB URL como argumento — funciona para local y producción sin modificar código",
      "Script de seed usa CREATE TABLE IF NOT EXISTS para ser idempotente — no falla si la tabla ya existe",
      "Script de seed registrado en package.json: 'db:seed': 'node db/populatedb.js'",
      "Base de datos de desarrollo, pruebas y producción completamente separadas — nunca mezclarlas",
      "Selección dinámica de DB según NODE_ENV: development → DATABASE_URL, test → TEST_DATABASE_URL",
      "GRANT ALL PRIVILEGES correctamente asignado al rol sobre la base de datos que usará la app",
      "Búsquedas implementadas en SQL (WHERE username ILIKE $1) — nunca filtrar con JavaScript sobre todos los registros",
    ],
  },
  {
    section: "🔷 Prisma ORM",
    color: "#818CF8",
    items: [
      "Prisma inicializado en modo JavaScript: npx prisma init --datasource-provider postgresql --output ../generated/prisma --generator-provider prisma-client-js",
      "Archivo de configuración nombrado prisma.config.js (no .ts) — el proyecto usa JavaScript puro",
      "PrismaClient importado desde la ruta generada con extensión .js explícita: import { PrismaClient } from '../generated/prisma/client.js'",
      "PrismaClient instanciado como singleton en lib/prisma.js y exportado — nunca instanciar un cliente por request",
      "Schema de Prisma en prisma/schema.prisma define todos los modelos, tipos, relaciones y constraints",
      "Relaciones entre modelos definidas en el schema con @relation — Prisma gestiona los joins automáticamente",
      "npx prisma generate ejecutado tras cada cambio al schema — regenera el cliente tipado",
      "npx prisma migrate dev para aplicar cambios del schema a la DB en desarrollo — genera SQL y lo ejecuta",
      "Carpeta migrations/ commiteada en Git — es el historial versionado de cambios a la DB",
      "Nunca editar la base de datos manualmente en producción — siempre usar migraciones de Prisma",
      "Prisma Studio (npx prisma studio) disponible para inspeccionar datos en desarrollo — no usarlo en producción",
      "prisma.entidad.findMany(), .findUnique(), .create(), .update(), .delete() para operaciones CRUD estándar",
      "select: { campo: true } en queries de Prisma para traer solo los campos necesarios — nunca exponer password en respuestas",
      "where: { campo: valor } para filtros; orderBy y take/skip para ordenamiento y paginación",
      "prisma.$transaction([]) para operaciones atómicas que deben ejecutarse todas o ninguna",
      "prisma.$queryRaw y prisma.$executeRaw disponibles para queries complejas que Prisma no puede expresar",
      "Prisma ORM no soporta columnas IDENTITY de PostgreSQL — usa Serial Types internamente, tenerlo en cuenta al migrar",
      "Instalar extensión oficial de Prisma en VS Code: syntax highlighting, autocompletado e IntelliSense en el schema",
      "generated/prisma/ en .gitignore — el cliente se regenera con prisma generate, no se versiona",
    ],
  },
  {
    section: "🔐 Autenticación & Autorización",
    color: "#FBBF24",
    items: [
      "bcryptjs instalado y usado para hashear contraseñas con factor 10 mínimo: bcrypt.hash(password, 10) — NUNCA almacenar texto plano",
      "bcrypt.compare(plainText, hash) usado en LocalStrategy para verificar — nunca comparar strings directamente",
      "El salt se almacena automáticamente dentro del hash de bcryptjs — no hace falta guardarlo por separado",
      "Usuarios creados antes de agregar bcrypt ya no son compatibles — implementar desde el inicio del proyecto",
      "Passport configurado con LocalStrategy: busca usuario en DB, verifica password con bcrypt, llama done(null, user) o done(null, false)",
      "passport.serializeUser guarda solo el id en la sesión — no el objeto completo del usuario",
      "passport.deserializeUser recupera el usuario completo de la DB por id y lo adjunta a req.user en cada request",
      "Orden de middleware en app.js es crítico: express-session → passport.session() → express.urlencoded → rutas",
      "express-session configurado con secret desde .env, resave: false y saveUninitialized: false",
      "connect-pg-simple usado para almacenar sesiones en PostgreSQL en producción — nunca en memoria (se pierde al reiniciar)",
      "La tabla de sesiones de connect-pg-simple debe crearse manualmente antes de arrancar la app",
      "passport.authenticate('local', { successRedirect, failureRedirect }) maneja login sin necesidad de código adicional",
      "req.logout() con callback para el endpoint de cierre de sesión — manejar el error con next(err) si existe",
      "currentUser disponible globalmente en todas las vistas mediante res.locals: app.use((req, res, next) => { res.locals.currentUser = req.user; next(); })",
      "Vistas condicionadas con locals.currentUser — mostrar contenido diferente según si el usuario está autenticado",
      "JWT firmado con secret desde variable de entorno (nunca hardcodeado) para APIs sin estado",
      "Expiración del token configurada: access token 1h, refresh token 7d como mínimo",
      "Middleware de verificación JWT aplicado a todas las rutas protegidas antes del controlador",
      "Passport JWTStrategy como alternativa a sesiones para APIs REST puras",
      "Separar autenticación (¿quién eres?) de autorización (¿qué puedes hacer?) — middlewares independientes",
      "Nunca exponer el hash de la contraseña en ninguna respuesta de la API — excluirlo explícitamente en todas las queries",
    ],
  },
  {
    section: "✅ Validación & Sanitización",
    color: "#F87171",
    items: [
      "express-validator instalado y sus funciones importadas: { body, validationResult, matchedData }",
      "Todo input de usuario validado con body() / param() / query() antes de llegar al controlador",
      "Las reglas de validación se definen como array de middlewares y se pasan a la ruta: router.post('/create', validateUser, controller)",
      "Encadenamiento de validaciones con mensajes únicos por fallo: .isAlpha().withMessage('...').isLength({min,max}).withMessage('...')",
      ".trim() aplicado en campos de texto para eliminar espacios sobrantes antes de validar",
      "Campos opcionales marcados con .optional({ values: 'falsy' }) — no valida si el valor es falsy",
      "validationResult(req) ejecutado al inicio del controlador — si errors no está vacío, responder 400 y hacer return",
      "matchedData(req) para obtener en el controlador solo los datos que pasaron la validación y sanitización — nunca req.body directamente",
      "Sanitización ≠ escape de salida: no usar .escape() al recibir datos — solo escapar en el momento de renderizar la salida",
      "En vistas EJS usar siempre <%= %> (escapado) y nunca <%- %> con datos de usuario — previene XSS",
      "Los errores de validación se devuelven con errors.array() y se renderizan en la vista — el usuario ve qué campo falló y por qué",
      "Datos de formularios GET disponibles en req.query (búsquedas); datos de formularios POST en req.body",
      "express.urlencoded({ extended: true }) registrado como middleware global para parsear formularios HTML",
      "Mensajes de error descriptivos, localizados y útiles para el usuario — nunca mensajes técnicos o genéricos",
    ],
  },
  {
    section: "📋 Manejo de Formularios & Patrón PRG",
    color: "#FCD34D",
    items: [
      "Patrón Post/Redirect/Get (PRG) implementado en todos los endpoints POST: procesar → res.redirect() — nunca res.render() tras un POST exitoso",
      "El atributo name de cada input define la key en req.body — verificar que coincide con lo que el controlador espera",
      "Formularios de creación/actualización usan method='POST'; formularios de búsqueda usan method='GET'",
      "Formularios de eliminación usan method='POST' con acción a /:id/delete — los navegadores no soportan DELETE en formularios HTML",
      "El atributo action del formulario apunta exactamente al endpoint del router: /api/v1/users/:id/update",
      "Al fallar la validación, re-renderizar el formulario con status 400 + los errores + los valores previos del usuario (no perder lo que escribió)",
      "Nunca confiar en la validación del lado del cliente (HTML required, type='email'…) — siempre revalidar en el servidor",
      "Para APIs REST puras los formularios no aplican, pero la validación del body JSON sigue siendo obligatoria con las mismas reglas",
    ],
  },
  {
    section: "⚠️ Manejo de Errores",
    color: "#FB923C",
    items: [
      "Middleware de error global con exactamente 4 parámetros (err, req, res, next) — registrado como último middleware en app.js",
      "Clases de error personalizadas en src/errors/: NotFoundError, ValidationError, UnauthorizedError, ConflictError",
      "Cada clase de error extiende Error y define statusCode y name — permite que el error handler responda con el código correcto",
      "El error handler lee err.statusCode: res.status(err.statusCode || 500).json({...}) — el 500 es el fallback para errores no controlados",
      "En Express v5 los async controllers propagan errores automáticamente — en Express v4 usar try/catch y llamar next(err)",
      "throw new CustomError() dentro de un controlador es suficiente — no necesitas res.status() en el controlador si lanzas el error",
      "Siempre hacer return después de enviar una respuesta de error en el controlador para evitar ejecutar código posterior",
      "Nunca exponer err.stack en producción — solo en development con NODE_ENV check",
      "Log de todos los errores en servidor: console.error(err) en el error handler como mínimo, idealmente con pino o winston",
      "Códigos HTTP correctos y semánticos: 200, 201, 204, 400, 401, 403, 404, 409, 422, 500",
      "El mensaje de error al cliente debe ser descriptivo pero sin revelar detalles internos del sistema",
      "Los errores de validación (422) deben incluir qué campos fallaron y por qué — no solo 'Error de validación'",
    ],
  },
  {
    section: "🔒 Seguridad & CORS",
    color: "#60A5FA",
    items: [
      "helmet() aplicado como primer middleware global — configura headers HTTP de seguridad automáticamente",
      "CORS habilitado con el paquete 'cors' de Express — nunca implementar CORS manualmente",
      "En desarrollo: app.use(cors()) permite todos los orígenes para facilitar el trabajo local",
      "En producción: cors({ origin: 'https://tufrontend.com' }) — whitelist explícita de orígenes permitidos",
      "CORS configurado antes que las rutas en el stack de middlewares",
      "CORS por ruta específica cuando solo algunos endpoints deben ser públicos: router.get('/public', cors(), handler)",
      "La Política del Mismo Origen (SOP) del navegador es la razón por la que se necesita CORS — es una medida de seguridad, no un bug",
      "Backend y frontend en dominios diferentes requieren CORS habilitado — es el caso estándar en arquitecturas Jamstack",
      "Rate limiting con express-rate-limit en endpoints de autenticación — previene ataques de fuerza bruta",
      "SSL/TLS habilitado en producción — el PaaS suele gestionarlo, verificar que la app responde en HTTPS",
      "Variables sensibles en .env — nunca en el código fuente ni en el historial de Git",
      "express-session con secret desde .env y saveUninitialized: false",
      "Sanitización contra SQL injection — Prisma usa queries parametrizadas, pg usa $1/$2",
      "No versionar secrets en Git — verificar con git log que ningún commit contiene credenciales",
      "ALLOWED_ORIGINS como variable de entorno — permite cambiar dominios permitidos sin tocar el código",
    ],
  },
  {
    section: "🧪 Testing",
    color: "#00D4FF",
    items: [
      "Test unitario por cada función de servicio y helper",
      "Test de integración con Supertest para cada endpoint (GET, POST, PUT, DELETE)",
      "Tests de autenticación: rutas protegidas retornan 401 sin token",
      "Tests de validación: inputs inválidos retornan 400 con mensaje descriptivo",
      "Tests con base de datos de prueba — nunca afectar datos de producción",
      "Coverage mínimo del 80% (jest --coverage)",
      "Script npm test configurado con --setupFiles dotenv/config",
    ],
  },
  {
    section: "⚡ Performance & Cache",
    color: "#E879F9",
    items: [
      "apicache en endpoints GET de alto tráfico (cache de 2 min mínimo)",
      "Paginación obligatoria en todos los endpoints de colección",
      "Índices de DB en columnas de filtros frecuentes",
      "Selección de campos específicos en Prisma (nunca select: *)",
      "Pool de conexiones configurado en Prisma/pg",
    ],
  },
  {
    section: "📄 Documentación",
    color: "#A78BFA",
    items: [
      "Swagger/OpenAPI configurado en /api/docs (swagger-jsdoc + swagger-ui-express)",
      "Todos los endpoints documentados: ruta, método, params, body, responses",
      "Schemas de datos documentados con ejemplos",
      "README.md con: descripción, instalación, variables de entorno, scripts npm",
      "Colección Postman/Insomnia exportada en el repo",
    ],
  },
  {
    section: "🚀 Despliegue & Producción",
    color: "#4ADE80",
    items: [
      "Script 'start' en package.json ejecuta node server.js (sin --watch ni nodemon) — el PaaS usará este comando",
      "Script 'dev' separado del 'start': nodemon o node --watch solo en desarrollo",
      "Versión de Node especificada en package.json bajo 'engines': { \"node\": \">=18\" } para garantizar compatibilidad con el host",
      "Puerto leído exclusivamente desde process.env.PORT — los PaaS asignan su propio puerto y no usarán el hardcodeado",
      "Todas las variables de entorno configuradas en el dashboard del PaaS (Railway, Render, Koyeb…) — nunca subir .env al repo",
      "DATABASE_URL de producción apunta a la base de datos del PaaS, no a localhost",
      "NODE_ENV=production configurado en el entorno del PaaS — activa optimizaciones y oculta stack traces",
      "Revisar los build logs completos tras cada deploy: el error aparece claramente en el flujo de salida",
      "Ante un error 500 post-deploy, abrir los application logs del PaaS y reproducir el error recargando la página",
      "Nunca exponer stack traces ni mensajes de error internos al cliente en producción (NODE_ENV check en el error handler)",
      "Migraciones de base de datos ejecutadas antes o durante el deploy — no después: configurar como release command en el PaaS",
      "Usar git log para identificar qué cambio introdujo un bug, git checkout para volver a la última versión funcional",
      "Las credenciales de base de datos de producción nunca aparecen en el código fuente ni en el historial de Git",
      "GitHub Pages / Netlify / Vercel NO son opciones para backends Node.js — requieren un PaaS (Railway, Render, Koyeb…)",
      "Verificar que la aplicación no crashea en arranque: app.listen() con callback que lanza el error si existe",
    ],
  },
  {
    section: "🔧 DevOps & Flujo de Trabajo",
    color: "#34D399",
    items: [
      "Scripts npm definidos: dev, start, test, migrate, seed",
      "Script 'dev' usa node --watch (built-in desde Node 18) o Nodemon — nunca en producción",
      "Nodemon si se usa, solo como devDependency — nunca en el script 'start' de producción",
      "package.json con engines: { node: '>=18' }",
      "Archivo .nvmrc o .node-version para fijar versión de Node",
      "Variables de entorno documentadas en .env.example (sin valores reales)",
      "Git hooks con husky: lint y tests antes de cada commit",
      "ESLint + Prettier configurados para estilo de código uniforme",
    ],
  },
];
