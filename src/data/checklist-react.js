// Checklist generada a partir del curso The Odin Project – React con Next.js
// Cada artículo del curso se convierte en una sección con tareas accionables.

export const checklistReact = [
  {
    section: "⚙️ Configuración del Entorno React",
    color: "#61DAFB",
    items: [
      "Instalar la versión LTS más reciente de Node.js — verificar con `node -v` antes de crear el proyecto",
      "Entender las diferencias entre las formas de iniciar un proyecto React: CDN, Vite, Gatsby, Next.js, CRA (deprecado)",
      "Comprender por qué se necesitan toolchains (gestor de paquetes, bundler, compilador) y no construir uno desde cero",
      "Saber que Create React App (CRA) está deprecado desde 2023 — no usarlo en proyectos nuevos",
      "Elegir la toolchain adecuada según el caso: Vite para SPAs, Next.js para apps con SSR/SSG",
      "Usar Vite para prototipado y aprendizaje: `npm create vite@latest my-app -- --template react`",
      "Para proyectos con Next.js: `npx create-next-app@latest my-app` con las opciones que correspondan al proyecto",
      "Responder correctamente las preguntas del CLI de Next.js: TypeScript (No si usas JS), ESLint (Yes), Tailwind (según preferencia), App Router (Yes para Next.js 13+)",
      "Confirmar que el servidor de desarrollo levanta sin errores — verificar en `http://localhost:3000` (Next.js) o `http://localhost:5173` (Vite)",
      "Familiarizarse con los archivos generados: `package.json`, `next.config.js` / `vite.config.js`, `.gitignore`, `README.md`",
    ],
  },
  {
    section: "📁 Estructura del Proyecto React/Next.js",
    color: "#A78BFA",
    items: [
      "Entender la carpeta `public/` — archivos estáticos accesibles directamente por URL (imágenes, íconos, `robots.txt`)",
      "Entender la carpeta `src/` — todo el código fuente de la aplicación vive aquí",
      "En Next.js con App Router: la carpeta `app/` contiene las rutas — cada carpeta es un segmento de ruta",
      "En Next.js, `app/layout.js` es el layout raíz — envuelve todas las páginas y es el lugar para fuentes, metadata global y providers",
      "En Next.js, `app/page.js` es el componente de la página raíz (`/`) — equivalente a `App.jsx` en Vite",
      "El archivo `main.jsx` / `main.js` es el punto de entrada en Vite — crea el root con `createRoot()` y renderiza `<App />`",
      "Mantener una estructura de carpetas clara: `components/`, `hooks/`, `lib/`, `data/`, `app/` (o `pages/` en Next.js Pages Router)",
      "Crear componentes en archivos `.jsx` (o `.tsx` si usas TypeScript) — un componente por archivo como convención",
      "Nunca poner lógica de negocio en los archivos de ruta de Next.js — extraerla a `lib/` o `services/`",
      "Agregar `components/` dentro de `app/` solo para componentes que son exclusivos de esa ruta — los compartidos van en `src/components/`",
    ],
  },
  {
    section: "🔍 Entender el Punto de Entrada",
    color: "#F97316",
    items: [
      "Leer y entender el archivo `main.jsx` generado por Vite — importa StrictMode, createRoot, App y el CSS global",
      "Entender `createRoot(document.getElementById('root'))` — busca el `<div id='root'>` en `index.html` para montar la app",
      "Entender el método `.render(<StrictMode><App /></StrictMode>)` — renderiza el árbol de componentes en el DOM",
      "Saber qué hace `<StrictMode>` — activa advertencias adicionales en desarrollo, no afecta producción",
      "En Next.js con App Router NO existe `main.jsx` — el framework gestiona el punto de entrada automáticamente",
      "En Next.js, `app/layout.js` exporta por defecto el `RootLayout` que recibe `{children}` — es el equivalente al `<StrictMode>` + root",
      "Entender que en Next.js las páginas son Server Components por defecto — añadir `'use client'` solo cuando se necesite interactividad",
      "Familiarizarse con la diferencia entre Server Components (sin estado, sin hooks) y Client Components (`'use client'`, con hooks y estado)",
    ],
  },
  {
    section: "🧹 Limpiar el Proyecto Inicial",
    color: "#34D399",
    items: [
      "Eliminar el contenido de ejemplo generado por Vite/Next.js — no dejar código de plantilla en producción",
      "En Vite: vaciar `App.jsx` y dejar un componente mínimo que renderice 'Hello, World!'",
      "En Next.js: reemplazar el contenido de `app/page.js` con un componente limpio propio",
      "Eliminar los estilos de ejemplo en `App.css` e `index.css` que no se vayan a usar",
      "Eliminar los archivos de assets de ejemplo (`react.svg`, `vite.svg`) si no se van a usar",
      "Verificar que el servidor de desarrollo sigue corriendo sin errores después de la limpieza",
      "Hacer un commit inicial del proyecto limpio antes de empezar a desarrollar features",
      "Confirmar que 'Hello, World!' (o el contenido mínimo propio) se muestra correctamente en el navegador",
    ],
  },
  {
    section: "🎨 Formateo & Calidad de Código",
    color: "#FBBF24",
    items: [
      "Instalar la extensión ESLint en VS Code — lint en tiempo real mientras escribes",
      "Instalar la extensión Prettier en VS Code — formateo automático al guardar",
      "Configurar VS Code para formatear al guardar: `\"editor.formatOnSave\": true` en `settings.json`",
      "Verificar que el `eslint.config.js` / `.eslintrc` generado está configurado con las reglas de React",
      "Instalar y configurar Prettier si no viene incluido: `npm install -D prettier eslint-config-prettier`",
      "Crear `.prettierrc` con las preferencias del proyecto (comillas simples, punto y coma, etc.)",
      "Asegurarse de que ESLint y Prettier no tienen reglas que conflictúen — usar `eslint-config-prettier` para ello",
      "Comprobar que `eslint-plugin-react-hooks` está activo — previene errores comunes con hooks",
    ],
  },
  {
    section: "🛠 React Developer Tools",
    color: "#FB7185",
    items: [
      "Instalar la extensión React Developer Tools en Chrome o Firefox",
      "Localizar la pestaña 'Components' en DevTools — permite inspeccionar el árbol de componentes en tiempo real",
      "Localizar la pestaña 'Profiler' en DevTools — permite medir el rendimiento de renders",
      "Usar la pestaña 'Components' para inspeccionar props y state de cualquier componente",
      "Practicar modificar props/state desde DevTools para entender cómo reacciona la UI",
      "Usar el ícono de selector (⊕) para hacer click en un elemento de la página y ver el componente que lo renderiza",
      "Entender que React DevTools solo funciona en modo desarrollo — en producción está limitado",
      "Instalar React DevTools como hábito desde el inicio del proyecto — no esperar a tener problemas para hacerlo",
    ],
  },
  {
    section: "🔗 Conectar Repo Local con GitHub",
    color: "#60A5FA",
    items: [
      "Crear un repositorio vacío en GitHub (sin README ni .gitignore — ya los tienes en el proyecto local)",
      "Inicializar Git en el proyecto local si no existe: `git init`",
      "Agregar el remote de GitHub: `git remote add origin <URL del repo>`",
      "Verificar que `.gitignore` incluye `node_modules/`, `.env`, `.next/` (Next.js) o `dist/` (Vite)",
      "Hacer el primer commit: `git add .` → `git commit -m 'feat: initial project setup'`",
      "Subir al repositorio: `git push -u origin main`",
      "Verificar en GitHub que todos los archivos subieron correctamente y que `node_modules/` NO aparece",
      "Proteger la rama `main` con reglas de branch protection en GitHub (opcional pero recomendado desde el inicio)",
    ],
  },
  {
    section: "🧩 Componentes de React",
    color: "#F97316",
    items: [
      "Entender qué es un componente: un fragmento de UI reutilizable e independiente — equivalente a una función de JavaScript que devuelve JSX",
      "Identificar cómo dividir una UI en componentes: App (raíz), Navbar, MainArticle, NewsletterForm, etc.",
      "Crear un componente funcional básico: una función de JavaScript que devuelve JSX — empezar por un `Greeting.jsx` propio",
      "El nombre del componente SIEMPRE en PascalCase (mayúscula inicial): `Greeting`, no `greeting` — React lo usa para distinguir componentes de etiquetas HTML",
      "Usar minúsculas a propósito para ver el error: `<greeting />` se interpreta como etiqueta HTML desconocida, no como componente",
      "Cada componente vive en su propio archivo dedicado dentro de `src/components/` — un componente por archivo",
      "Exportar el componente con `export default NombreComponente` al final del archivo para que otros puedan importarlo",
      "Importar el componente en el archivo que lo necesite: `import Greeting from './Greeting.jsx'` — con extensión `.jsx` explícita",
      "Desde React v17+ ya NO es necesario `import React from 'react'` en cada archivo — el nuevo JSX transform lo gestiona automáticamente",
      "Usar el componente en JSX con su nombre en PascalCase y como etiqueta auto-cerrada si no tiene hijos: `<Greeting />`",
      "Reemplazar `<App />` en `main.jsx` por el componente propio para verificar que se renderiza correctamente",
      "Practicar con un componente de contenido real: comida favorita, película, etc. — no quedarse en el 'Hello World'",
      "Probar `export` con nombre en lugar de `export default`: `export function Greeting() {}` → importar con `{ Greeting }`",
      "Entender la diferencia entre `export default` (un solo export por archivo, importación sin llaves) y named export (múltiples exports, importación con llaves `{}`)",
      "En Next.js con App Router, las páginas usan `export default` obligatoriamente — los componentes reutilizables pueden usar named exports",
    ],
  },
  {
    section: "🏷️ JSX — Sintaxis y Reglas",
    color: "#FBBF24",
    items: [
      "Entender qué es JSX: extensión de sintaxis de JavaScript que permite escribir markup similar a HTML dentro de archivos JS",
      "JSX NO es HTML — es azúcar sintáctico de `React.createElement()` que compila a objetos JavaScript planos",
      "JSX no es obligatorio en React, pero hace los componentes más concisos y visuales — úsalo siempre",
      "JSX une lógica de renderizado y markup en el mismo lugar (el componente) — esa es su ventaja frente a separar HTML/JS en archivos distintos",
      "REGLA 1 — Retornar un único elemento raíz: un componente no puede retornar múltiples elementos sin un contenedor",
      "Usar `<div>` como contenedor si necesitas un nodo real en el DOM, o `<>...</>` (Fragment) si no quieres nodo extra",
      "REGLA 2 — Cerrar todas las etiquetas explícitamente: `<input>` → `<input />`, `<li>` → `<li></li>` — no existen etiquetas auto-cerradas implícitas en JSX",
      "REGLA 3 — camelCase en la mayoría de atributos: `class` → `className`, `stroke-width` → `strokeWidth`, `for` → `htmlFor`",
      "No usar palabras reservadas de JS como atributos JSX: `class` es palabra reservada → usar `className`; `for` → `htmlFor`",
      "Atributos con guiones tampoco funcionan en JSX: `stroke-width` → `strokeWidth`, `font-size` → `fontSize`",
      "Practicar la conversión de HTML a JSX: envolver en raíz, cerrar tags, convertir atributos a camelCase — hacerlo en el entorno local",
      "Usar `react.new` en el navegador para probar JSX rápidamente sin necesidad de configurar un proyecto",
      "Leer la página oficial 'Writing Markup with JSX' de la documentación de React",
      "Leer la página oficial 'JavaScript in JSX with Curly Braces' de la documentación de React — introducción a `{}` para lógica dinámica",
      "Usar `{}` en JSX para insertar cualquier expresión JavaScript válida: variables, llamadas a funciones, operadores ternarios",
      "Distinguir cuándo el error viene de no tener raíz única vs. tag no cerrado vs. atributo incorrecto — React muestra el error específico en consola",
    ],
  },
  {
    section: "🔁 Técnicas de Renderizado",
    color: "#34D399",
    items: [
      "Renderizar listas con `.map()` dentro de JSX usando `{}` — nunca hardcodear elementos repetidos cuando hay un array de datos",
      "Cada elemento renderizado con `.map()` debe tener una prop `key` única — añadirla siempre aunque la lista sea estática por ahora",
      "La `key` debe ser un valor único y estable del dato (id, nombre único) — no usar el índice del array como key en listas dinámicas",
      "JSX puede renderizar arrays directamente: preparar el array de elementos fuera del return y pasarlo entre `{}` es equivalente a mapear inline",
      "Separar componentes de lista: `<List />` para el contenedor y `<ListItem />` para cada elemento — mejora la legibilidad y reutilización",
      "Pasar datos a componentes a través de `props` — son como argumentos de función pero con sintaxis de atributos HTML: `<List animals={animals} />`",
      "Acceder a las props dentro del componente como `props.nombreProp` — el nombre de la prop lo defines tú al usarla: `<List animalList={animals} />` → `props.animalList`",
      "Desactivar la regla ESLint `react/prop-types` en `eslint.config.js` mientras aprendes — evita el warning 'missing in props validation'",
      "RENDERIZADO CONDICIONAL — Operador ternario: `condición ? <Elemento /> : null` — retornar `null` no renderiza nada",
      "RENDERIZADO CONDICIONAL — Operador `&&`: `condición && <Elemento />` — si la condición es true renderiza el elemento, si es false se ignora",
      "PITFALL con `&&`: nunca poner un número en el lado izquierdo — `0 && <X />` renderiza `0` en lugar de nada porque 0 es falsy pero React lo muestra",
      "RENDERIZADO CONDICIONAL — `if` / `if-else` como early return: ideal para guardianes al inicio del componente antes del return principal",
      "Patrón 'loading state': `if (!props.data) return <div>Loading...</div>` — se ejecuta mientras los datos no están disponibles (ej: fetch a API)",
      "Patrón 'empty state': `if (props.data.length === 0) return <div>No hay resultados</div>` — siempre considerar el caso de lista vacía",
      "Preferir `if/else` con early returns sobre ternarios anidados — los ternarios encadenados son difíciles de leer y mantener",
      "Leer la guía oficial de React sobre Conditional Rendering y completar todos sus ejemplos",
      "Leer la guía oficial de React sobre Rendering Lists — no preocuparse aún por la sección de keys, se verá en la próxima lección",
    ],
  },
  {
    section: "🔑 Keys en React",
    color: "#A78BFA",
    items: [
      "Entender para qué sirve `key`: permite a React identificar qué elemento de una lista cambió para re-renderizar solo ese, no toda la lista",
      "Sin `key`, React re-renderiza toda la lista ante cualquier cambio — con `key` es eficiente y solo actualiza el elemento afectado",
      "Sintaxis: `key` se pasa como prop directamente al componente o elemento DOM: `<Component key={id} />` o `<li key={id}>`",
      "La mejor `key` es un identificador único y estable que venga de los datos — típicamente el `id` de la base de datos",
      "Generar ids propios con `crypto.randomUUID()` al definir la estructura de datos — almacenar el id EN el objeto, no generarlo al renderizar",
      "ANTI-PATRÓN: nunca generar `key` durante el render — `key={crypto.randomUUID()}` o `key={Math.random()}` crea una key nueva en cada render, inutilizando su propósito",
      "El índice del array (`index`) como `key` solo es aceptable si la lista es completamente estática y nunca cambia de orden ni se elimina/inserta",
      "Usar índice como `key` en listas dinámicas provoca bugs sutiles: al eliminar o reordenar elementos los índices cambian y React confunde los items",
      "Ver el video corto que demuestra el anti-patrón de index como key para entender visualmente el problema",
      "`key` no es una prop accesible dentro del componente — no se puede leer con `props.key` — si necesitas el id dentro del componente, pásalo también como otra prop",
      "Las `keys` deben ser únicas entre hermanos (siblings) del mismo nivel — no necesitan ser globalmente únicas en toda la app",
      "El uso de `key` no se limita a listas — hay casos avanzados donde se usa para forzar el reinicio de un componente al cambiar su key",
      "Leer la sección oficial de React docs sobre keys para consolidar el concepto",
    ],
  },
  {
    section: "📦 Props — Pasar Datos entre Componentes",
    color: "#FB7185",
    items: [
      "Entender el flujo de datos unidireccional de React: los datos viajan de componente padre a hijo a través de props — nunca al revés directamente",
      "Un cambio en una prop solo afecta al componente hijo que la recibe — no al padre ni a componentes hermanos",
      "Pasar props al componente hijo con sintaxis de atributos HTML: `<Button text='Click Me!' color='blue' fontSize={12} />`",
      "Strings se pasan con comillas `text='Hola'`; números, booleanos, objetos y arrays se pasan con llaves `fontSize={12}` `active={true}`",
      "Acceder a las props dentro del componente funcional con el objeto `props`: `props.text`, `props.color`, `props.fontSize`",
      "PATRÓN: desestructurar las props en los parámetros de la función para código más conciso: `function Button({ text, color, fontSize })`",
      "Definir valores por defecto directamente en la desestructuración: `function Button({ text = 'Click Me!', color = 'blue', fontSize = 12 })`",
      "Los valores por defecto protegen el componente de recibir `undefined` cuando una prop no se pasa",
      "Conocer `Button.defaultProps = { ... }` para reconocerlo en codebases legacy — en componentes funcionales modernos preferir defaults en parámetros",
      "Pasar funciones como props para que el hijo pueda comunicarse hacia el padre: `<Button handleClick={handleButtonClick} />`",
      "Pasar SOLO la referencia de la función, nunca la llamada: `handleClick={handleButtonClick}` ✅ — `handleClick={handleButtonClick()}` ❌ (se ejecuta al renderizar)",
      "Cuando la función necesita argumentos, envolverla en una arrow function: `handleClick={() => handleButtonClick('https://...')}` para evitar ejecución inmediata",
      "Los estilos inline se pueden generar dinámicamente con props: `const buttonStyle = { color: props.color, fontSize: props.fontSize + 'px' }`",
      "Cada instancia del componente puede recibir props distintas — un solo componente `<Button />` reemplaza múltiples variantes `Button1`, `Button2`, `Button3`",
      "Las props son de solo lectura dentro del componente hijo — nunca mutar `props.x = valor` directamente",
      "Leer la documentación oficial de React 'Passing Props to a Component' y experimentar con distintos valores en los ejemplos de código",
    ],
  },
  {
    section: "🧠 Introducción al Estado (State)",
    color: "#00D4FF",
    items: [
      "Leer el artículo 'What is State?' de Academind antes de empezar — da el contexto conceptual necesario",
      "Entender qué es el estado: la 'memoria' de un componente — información que el componente recuerda entre renders",
      "Distinguir estado de props: las props vienen del padre (externas, de solo lectura); el estado es interno y el componente lo gestiona",
      "Importar el hook `useState` desde React: `import { useState } from 'react'`",
      "Sintaxis estándar de `useState`: `const [valorEstado, setValorEstado] = useState(valorInicial)` — desestructuración de array",
      "El primer elemento del array es el valor actual del estado; el segundo es la función para actualizarlo",
      "Nombrar la función actualizadora con prefijo `set`: `setColor`, `setCount`, `setIsOpen` — convención estándar de React",
      "El valor inicial de `useState` solo se usa en el primer render — en renders posteriores React provee el estado más reciente ignorando el inicial",
      "Añadir múltiples variables de estado con múltiples llamadas a `useState` — cada una es independiente",
      "Llamar a la función `set` (ej: `setColor('red')`) desencadena un re-render del componente con el nuevo valor",
      "CÓMO FUNCIONA EL RE-RENDER: al cambiar el estado, React vuelve a ejecutar la función del componente completa para calcular el nuevo output",
      "React compara el nuevo Virtual DOM con el anterior (reconciliación) y aplica al DOM real solo los cambios mínimos necesarios",
      "El Virtual DOM es una representación ligera del DOM real que React usa para calcular diffs de forma eficiente",
      "REGLAS DE LOS HOOKS — Solo llamar hooks en el nivel superior del componente: nunca dentro de `if`, `for`, funciones anidadas, etc.",
      "REGLAS DE LOS HOOKS — Solo llamar hooks desde componentes funcionales de React (o custom hooks) — nunca desde funciones JS normales",
      "Todos los hooks se reconocen por el prefijo `use`: `useState`, `useEffect`, `useRef`, etc.",
      "Leer la doc oficial 'State: A Component\\'s Memory' y 'Render and Commit' de la documentación de React",
      "Leer el artículo sobre el algoritmo de reconciliación de React para entender el proceso a fondo",
      "Ejercicio práctico: añadir una variable de estado que cuente cuántas veces cambia el color de fondo — mostrar el contador en pantalla",
    ],
  },
  {
    section: "🔄 Estado Avanzado (More On State)",
    color: "#E879F9",
    items: [
      "REGLA: no guardar en estado valores que se puedan calcular a partir de estado, props u otros valores existentes — derivarlos directamente en el render",
      "REGLA: nunca mutar el estado directamente — siempre crear un nuevo objeto/array y pasarlo a la función `set`",
      "Para objetos en estado: usar spread para copiar y sobrescribir solo la propiedad que cambia: `setPerson({ ...person, age: person.age + 1 })`",
      "Para arrays en estado: usar métodos que devuelven nuevo array (`map`, `filter`, `concat`, spread) — nunca `push`, `splice` o mutación directa",
      "React usa `Object.is()` internamente para comparar el estado anterior con el nuevo — pasar el mismo objeto de referencia NO dispara un re-render",
      "Los objetos y arrays anidados en estado requieren copiar todos los niveles — con nesting profundo considerar librerías como Immer",
      "Las actualizaciones de estado son ASÍNCRONAS: llamar a `setState` no actualiza la variable en la misma ejecución — el nuevo valor llega en el próximo render",
      "El estado es una 'snapshot' (foto fija) durante cada render — `person` tendrá el mismo valor antes y después de llamar a `setPerson` dentro del mismo handler",
      "Llamar a `setState` fuera de un event handler (directamente en el cuerpo del componente) causa un bucle infinito de re-renders — React lo detecta y lanza error",
      "Llamar a `setState` dos veces seguidas con un valor no actualiza el estado dos veces — ambas operaciones usan el mismo valor de la snapshot actual",
      "FUNCIÓN ACTUALIZADORA: pasar un callback a `setState` garantiza recibir el estado más reciente: `setPerson(prev => ({ ...prev, age: prev.age + 1 }))`",
      "Usar función actualizadora cuando se necesita actualizar el estado múltiples veces en el mismo handler basándose en el valor anterior",
      "React agrupa (batches) múltiples llamadas a `setState` en el mismo evento — el componente solo re-renderiza una vez, no una vez por cada `set`",
      "COMPONENTES CONTROLADOS: elementos HTML con estado propio (como `<input>`) se controlan vinculando su `value` a estado de React y actualizando con `onChange`",
      "Patrón de input controlado: `const [value, setValue] = useState('')` + `<input value={value} onChange={e => setValue(e.target.value)} />`",
      "Un input controlado garantiza que React siempre tiene el valor más reciente — imprescindible para validación, envío de formularios y sincronización entre componentes",
      "Leer la doc oficial 'State as a Snapshot', 'Choosing the State Structure' y 'Sharing State Between Components'",
      "Ejercicio: componente `Person` con dos inputs controlados (nombre y apellido) que actualizan un `<h1>` con el nombre completo en cada keystroke",
    ],
  },
  {
    section: "⚡ Efectos Secundarios — useEffect",
    color: "#38BDF8",
    items: [
      "Entender qué es un side-effect: cualquier interacción del componente con el mundo exterior — fetch a API, timers, manipulación del DOM, suscripciones",
      "Distinguir rendering code (puro, sin side-effects) de effects (sincronización con sistemas externos) y de event handlers (respuesta a acciones del usuario)",
      "Importar `useEffect` desde React: `import { useEffect } from 'react'`",
      "Sintaxis básica: `useEffect(() => { /* efecto */ })` — sin array de dependencias se ejecuta en CADA render",
      "Nunca poner `setInterval`, `fetch` u otros side-effects directamente en el cuerpo del componente — causan bucles infinitos de re-render",
      "ARRAY DE DEPENDENCIAS vacío `[]`: el efecto solo se ejecuta una vez al montar el componente — equivale al 'componentDidMount' de clases",
      "ARRAY DE DEPENDENCIAS con valores `[a, b]`: el efecto se ejecuta al montar Y cada vez que `a` o `b` cambien desde el último render",
      "Sin array de dependencias: el efecto corre en cada render — raramente es lo que se quiere",
      "Dejar que el linter (eslint-plugin-react-hooks) infiera las dependencias correctas — nunca suprimir los warnings de dependencias sin entenderlos",
      "FUNCIÓN DE LIMPIEZA (cleanup): retornar una función desde el callback del efecto — se ejecuta antes del siguiente efecto y al desmontar el componente",
      "Siempre limpiar timers (`clearInterval`/`clearTimeout`), suscripciones y event listeners en la función de cleanup — evita memory leaks",
      "StrictMode en desarrollo monta, desmonta y vuelve a montar los componentes — sirve para detectar efectos sin cleanup correcto",
      "CUÁNDO NO USAR useEffect — No usarlo para calcular valores derivados del estado: calcularlos directamente en el render (`const sum = a + b`)",
      "CUÁNDO NO USAR useEffect — No usarlo para manejar eventos del usuario: usar event handlers de React (`onChange`, `onClick`, etc.)",
      "CUÁNDO NO USAR useEffect — No usarlo para resetear estado basado en props: usar la prop `key` en el componente para forzar su reinicio",
      "CUÁNDO NO USAR useEffect — No usarlo para compartir estado entre componentes hermanos: elevar el estado al padre común (lifting state up)",
      "Un `useEffect` innecesario es code-smell: introduce bugs, dificulta el mantenimiento y causa problemas de rendimiento",
      "Pregunta de diagnóstico antes de añadir un `useEffect`: '¿Hay algún sistema externo (servidor, API, DOM del navegador) con el que necesito sincronizarme?' — si la respuesta es no, probablemente no necesitas el efecto",
      "Leer la doc oficial sobre el ciclo de vida de un componente y el rol de `useEffect` en él",
      "Leer la doc oficial 'You Might Not Need an Effect' — repasa todos los casos donde el efecto es innecesario",
      "Leer el artículo sobre el bucle infinito con useEffect — error clásico de principiantes",
    ],
  },
  {
    section: "🏛️ Componentes de Clase (Class Components)",
    color: "#FBBF24",
    items: [
      "Entender por qué existen los class components: antes de React 16.8 (feb 2019) los componentes funcionales no podían tener estado — los hooks cambiaron eso",
      "Reconocer class components en codebases legacy — es muy probable encontrarlos en proyectos de trabajo reales",
      "Estructura mínima: una clase que extiende `Component` importado de React: `import { Component } from 'react'` + `class MyComp extends Component {}`",
      "El constructor recibe `props` como parámetro y debe llamar a `super(props)` — esto permite acceder a las props con `this.props` en toda la clase",
      "Si el componente no tiene props ni estado, el constructor puede omitirse",
      "El JSX se retorna desde el método `render()` — es el único método obligatorio en un class component",
      "Acceder a las props dentro del componente con `this.props.nombreProp` — a diferencia de funcionales donde se desestructuran directamente",
      "El estado se inicializa en el constructor como objeto: `this.state = { campo: valorInicial }`",
      "Actualizar el estado con `this.setState({ campo: nuevoValor })` — nunca mutar `this.state` directamente",
      "Pasar función actualizadora a `setState` para basarse en el estado previo: `this.setState(state => ({ ...state, count: state.count + 1 }))`",
      "Los métodos de clase no están vinculados automáticamente — hacer bind en el constructor: `this.handleClick = this.handleClick.bind(this)`",
      "Alternativa al bind: definir los métodos como arrow functions — `handleClick = () => {}` — `this` queda vinculado automáticamente a la instancia",
      "Acceder al estado en el render con `this.state.campo` y usarlo en el JSX como expresión `{this.state.todos.map(...)}`",
      "Comparación clave: `useState` hook → `this.state` + `this.setState`; función del componente → método `render()`; destructuración de props → `this.props.x`",
      "Ejercicio: implementar botón de eliminar tarea — filtrar el array de todos en el estado para quitar el elemento seleccionado",
      "Ejercicio: implementar componente `Count` que muestra el número de todos en tiempo real — recibe el conteo como prop del componente padre",
      "Ejercicio avanzado: implementar botón de editar por tarea — reemplaza el texto por un input y cambia el botón a 'Resubmit' para guardar los cambios",
    ],
  },
  {
    section: "🔃 Métodos del Ciclo de Vida (Lifecycle Methods)",
    color: "#F97316",
    items: [
      "Entender las 3 etapas del ciclo de vida de un componente: Mounting (inserción en el DOM), Updating (re-render por estado/props) y Unmounting (eliminación del DOM)",
      "`render()` es el único método de ciclo de vida obligatorio en un class component — se ejecuta en mounting y en cada update",
      "`render()` debe ser puro: no modificar estado, no interactuar con el navegador directamente, devolver siempre el mismo output dado el mismo input",
      "`componentDidMount()` se ejecuta UNA vez después de que el componente se inserta en el DOM — lugar correcto para fetch de datos y operaciones que dependen del DOM",
      "`componentDidUpdate(prevProps, prevState)` se ejecuta después de cada re-render — usar condicionales para evitar bucles infinitos al actualizar estado dentro",
      "Patrón seguro en `componentDidUpdate`: `if (this.props.userId !== prevProps.userId) { this.fetchUserData(); }` — comparar prev vs actual antes de actuar",
      "`componentWillUnmount()` se ejecuta justo antes de que el componente sea desmontado y destruido — lugar correcto para limpiar timers, cancelar requests y eliminar event listeners",
      "Nunca llamar a `setState` dentro de `componentWillUnmount` — el componente está a punto de desaparecer, no tiene sentido actualizar su estado",
      "Revisar el diagrama visual del ciclo de vida de componentes React para ver la secuencia completa de métodos",
      "Leer la documentación oficial de class components desde `constructor(props)` hasta `componentWillUnmount()` — ignorar las APIs marcadas como deprecated",
      "EQUIVALENCIAS con useEffect: `[]` → `componentDidMount`; `[dep]` → `componentDidMount` + `componentDidUpdate` (solo cuando dep cambia); sin array → `componentDidMount` + `componentDidUpdate`; return function → `componentWillUnmount`",
      "Un solo `useEffect` con cleanup y array vacío equivale a `componentDidMount` + `componentWillUnmount` combinados",
      "En proyectos nuevos usar siempre componentes funcionales con hooks — los lifecycle methods de clase son conocimiento necesario para leer código legacy, no para escribir código nuevo",
    ],
  },
  {
    section: "🧪 Testing en React — Vitest + RTL",
    color: "#34D399",
    items: [
      "Entender por qué el testing de UI es valioso: los tests de lógica no garantizan que la UI muestre lo correcto ni que el usuario pueda interactuar como se espera",
      "En proyectos con Vite usar Vitest como test runner (no Jest directamente) — se integra nativamente con Vite y la configuración es compatible",
      "Seguir la guía de Robin Wieruch para configurar Vitest + React Testing Library (RTL) desde cero",
      "Instalar los paquetes necesarios: `npm install -D vitest @testing-library/react @testing-library/jest-dom @testing-library/user-event jsdom`",
      "Configurar `globals: true` en `vite.config.js` O importar explícitamente `describe`, `it`, `expect` desde `vitest` en cada test — ESLint no reconoce globals de Vitest sin configuración extra",
      "`@testing-library/react` provee `render` y `screen` — las dos herramientas principales para montar componentes y consultarlos en tests",
      "`@testing-library/jest-dom` agrega matchers semánticos: `toBeInTheDocument`, `toHaveTextContent`, `toBeDisabled`, etc.",
      "`@testing-library/user-event` provee la API `userEvent` para simular interacciones reales del usuario (click, type, keyboard, etc.)",
      "jsdom simula el DOM en memoria sin navegador real — permite parsear y consultar el árbol renderizado en los tests",
      "Patrón de test básico: `render(<App />)` → consultar con `screen.getBy*()` → hacer assertion con `expect()`",
      "Preferir `screen` para acceder a queries en lugar de desestructurar `render` — `screen` siempre tiene el DOM actualizado y no requiere mantenimiento",
      "PRIORIDAD DE QUERIES: preferir `ByRole` > `ByLabelText` > `ByPlaceholderText` > `ByText` > `ByTestId` — las queries por rol garantizan accesibilidad",
      "`getByRole('heading', { name: /our first test/i })` — usar `name` para mayor especificidad y regex con flag `i` para comparación case-insensitive",
      "Tipos de queries: `getBy*` (lanza error si no encuentra), `queryBy*` (devuelve null si no encuentra), `findBy*` (async, espera a que aparezca)",
      "Simular eventos con `userEvent`: `const user = userEvent.setup()` → `await user.click(button)` — los callbacks de tests con userEvent deben ser `async`",
      "RTL desmonta el componente después de cada test — renderizar de nuevo en cada test o usar una función `setup` personalizada para tests repetitivos",
      "SNAPSHOT TESTS: `expect(container).toMatchSnapshot()` genera un archivo `.snap` con el HTML del componente y falla si cambia algo",
      "Los snapshots detectan cambios inesperados pero pueden generar falsos positivos (bug no detectado) y falsos negativos (cambio legítimo rompe el test)",
      "No abusar de snapshot tests — usarlos de forma selectiva, no como sustituto de assertions semánticas",
      "Leer el artículo 'Testing Implementation Details' de Kent C. Dodds — evitar testear implementación interna, testear comportamiento observable",
      "Revisar el cheatsheet de RTL con todos los métodos de query disponibles — usar `data-testid` como último recurso cuando ningún otro método aplica",
      "Leer la API de `userEvent` para conocer todas las interacciones simulables: click, dblClick, type, keyboard, hover, etc.",
    ],
  },
  {
    section: "🎭 Mocking — Callbacks y Componentes",
    color: "#C084FC",
    items: [
      "Entender qué es un mock en el contexto de testing: una función o módulo simulado que reemplaza la implementación real para aislar lo que se está probando",
      "Usar `vi.fn()` de Vitest para crear una función mock: `const onClick = vi.fn()` — registra si fue llamada, cuántas veces y con qué argumentos",
      "Pasar la función mock como prop al componente: `render(<CustomButton onClick={onClick} />)` — el componente no sabe que es un mock",
      "Assertion de que fue llamada: `expect(onClick).toHaveBeenCalled()` — verificar invocación tras simular el evento",
      "Assertion de que NO fue llamada: `expect(onClick).not.toHaveBeenCalled()` — verificar que el evento no se disparó sin interacción",
      "Configurar `userEvent.setup()` ANTES de renderizar el componente — es la práctica recomendada por RTL",
      "Preferir configurar mocks dentro de cada bloque `it` en lugar de en `beforeEach` — mejora legibilidad, elimina contexto oculto y evita fugas entre tests",
      "Usar `beforeEach` para mocks solo cuando el archivo de test es muy largo y la preparación ocupa decenas de líneas",
      "Si varios tests repiten la misma preparación, extraer una función `setup()` personalizada dentro del archivo de test — no usar `beforeEach`",
      "Mockear módulos completos con `vi.mock('../ruta/modulo', () => implementacionSimulada)` — útil para componentes hijos o librerías externas complejas",
      "Al mockear un componente hijo, renderizar solo lo mínimo necesario para verificar el comportamiento del componente padre — no copiar toda la lógica del hijo",
      "Usar `data-testid` en componentes mockeados para identificarlos en las queries: `<div data-testid='submission'>{id}</div>`",
      "RTL usa `data-testid` por defecto — algunos codebases legacy usan `data-test-id` (con guión) — no confundirlos",
      "Patrón Arrange-Act-Assert (AAA): organizar datos y mocks → actuar (simular evento) → hacer assertions — aplicarlo en todos los tests para mayor legibilidad",
      "Leer el artículo sobre cómo mockear componentes hijos en React — explica en detalle la técnica y cuándo aplicarla",
      "Ver el tutorial de Academind sobre testing en React — la API `userEvent` es síncrona en el tutorial pero en la versión actual es async, adaptarlo",
    ],
  },
  {
    section: "🔎 Verificación de Tipos con PropTypes",
    color: "#FB923C",
    items: [
      "Entender qué es la verificación de tipos: proceso que valida que las props recibidas por un componente sean del tipo correcto — detecta errores en desarrollo, no en producción",
      "IMPORTANTE: PropTypes y defaultProps están deprecados a partir de React 19 — usar React ≤18 para esta lección o considerar TypeScript en proyectos nuevos",
      "Instalar la librería: `npm install --save prop-types` — ya no viene incluida en React desde v15.5",
      "Importar en el componente: `import PropTypes from 'prop-types'`",
      "Definir propTypes en el componente: `NombreComponente.propTypes = { nombre: PropTypes.string }` — como propiedad estática del componente",
      "Si la prop es incorrecta, React muestra una advertencia en la consola en modo desarrollo — no lanza errores, solo warnings",
      "Usar `.isRequired` para marcar una prop como obligatoria: `PropTypes.string.isRequired` — si no se pasa, aparece warning",
      "Tipos primitivos disponibles: `PropTypes.string`, `PropTypes.number`, `PropTypes.bool`, `PropTypes.func`, `PropTypes.symbol`",
      "Tipos de estructura: `PropTypes.array`, `PropTypes.object`, `PropTypes.node` (todo lo renderable), `PropTypes.element` (elemento React)",
      "Tipos compuestos: `PropTypes.arrayOf(PropTypes.string)`, `PropTypes.shape({ id: PropTypes.number, name: PropTypes.string })`",
      "Usar `PropTypes.oneOf(['admin', 'user'])` para limitar a un conjunto de valores; `PropTypes.oneOfType([PropTypes.string, PropTypes.number])` para múltiples tipos",
      "Definir valores por defecto con `defaultProps`: `NombreComponente.defaultProps = { name: 'Zach' }` — se aplican cuando la prop no se pasa",
      "Los valores pasados explícitamente siempre prevalecen sobre los `defaultProps`",
      "Alternativa moderna a `defaultProps`: valores por defecto en la desestructuración de parámetros — preferir este enfoque en componentes funcionales",
      "Se pueden definir validadores personalizados en PropTypes: función que recibe `(props, propName, componentName)` y retorna un `Error` si falla",
      "En proyectos de producción modernos considerar TypeScript en lugar de PropTypes — ofrece type-safety en tiempo de compilación, no solo en runtime",
      "No aprender TypeScript mientras se está aprendiendo React — consolidar los fundamentos de JavaScript primero, luego refactorizar proyectos existentes",
      "Leer la documentación oficial de PropTypes para ver todos los tipos disponibles y sus opciones",
      "Leer la guía completa de validación de props en LogRocket para casos de uso reales y validadores personalizados",
    ],
  },
];
